#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @n: The integer to be operated
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
